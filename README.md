# 💖 Cute AI Text Detector ✍️

A fancy and cute web application built with Streamlit to detect whether a text is written by a human or generated by AI. This app uses a pre-trained RoBERTa model from the Hugging Face library to perform the classification.

## ✨ Features

- **AI vs. Human Text Detection**: Analyzes input text to determine if it was written by a human or an AI.
- **Cute and Interactive UI**: A visually appealing and user-friendly interface with a pastel color scheme, custom fonts, and emojis.
- **Model Information**: Displays information about the underlying AI model.
- **Confidence Score**: Shows the confidence level of the prediction.
- **Responsive Design**: The app is designed to work on different screen sizes.

## 📸 Screenshot

*(A screenshot of the application will be added here)*

## 🛠️ Technologies Used

- **Python**: The core programming language.
- **Streamlit**: For creating the interactive web application.
- **Hugging Face Transformers**: For accessing the pre-trained RoBERTa model.
- **PyTorch**: As a backend for the Transformers library.
- **HTML/CSS**: For custom styling and layout.

## 🚀 Setup and Usage

To run this application locally, follow these steps:

1. **Clone the repository:**
   ```bash
   git clone https://github.com/your-username/your-repository-name.git
   cd your-repository-name
   ```

2. **Install the dependencies:**
   Make sure you have Python 3.11 or higher installed. Then, install the required packages using pip:
   ```bash
   pip install streamlit transformers torch
   ```

3. **Run the application:**
   ```bash
   streamlit run app.py
   ```
   The application will open in your default web browser.

## 👤 Author

- **Anny**

---

*This README was generated by an AI assistant.*
